```
Usage:
  -help
        Displays help
  -interval duration
        Interval for send messages (default 1s)
  -rps uint
        Requests per second will be sent (default 100)
  -timeout duration
        Timeout for every HTTP requests (default 5s)
  -url string
        URL for sending requests (required)
```